# MAE159 Project WrightCamryn

This is the project directory for the MAE159 Subsonic Transport aircraft design project.

## Directory Structure

*   `main.m`: Main MATLAB script that initializes the path and reads project metadata.
*   `metadata.json`: JSON file containing project metadata (name, author, parameters).
*   `modules/`: Directory containing project modules.
    *   `modules/drag/`: Drag calculation module.
        *   `calculate_drag.m`: MATLAB function to calculate drag coefficient CD.

## Usage

1.  Open MATLAB.
2.  Navigate to this directory.
3.  Run `main`.
